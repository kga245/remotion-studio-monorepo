import {RemotionPages} from './scenes/RemotionPages';
import {projectConfig as config} from './project.config';

const defaults = {};

export default {
  id: 'RemotionRev-RemotionPages',
  tags: ['rev', 'pages'],
  component: RemotionPages,
  config,
  defaults,
};
