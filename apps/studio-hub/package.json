{
  "name": "@studio/studio-hub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gen:hub": "node --import tsx ../../scripts/gen-root.ts",
    "sync:public": "node --import tsx ../../scripts/sync-public.ts",
    "dev": "pnpm run gen:hub --silent -- $HUB_ARGS && pnpm run sync:public --silent && remotion studio",
    "dev:auto": "pnpm run gen:hub --silent -- $HUB_ARGS && pnpm run sync:public --silent && remotion studio",
    "dev:port": "pnpm run gen:hub --silent -- $HUB_ARGS && pnpm run sync:public --silent && remotion studio --port=$HUB_PORT",
    "render": "pnpm run gen:hub --silent -- $HUB_ARGS && pnpm run sync:public --silent && remotion render",
    "list": "node --import tsx ../../scripts/gen-root.ts --list"
  },
  "dependencies": {
    "remotion": "^4.0.350",
    "@remotion/cli": "^4.0.350",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
