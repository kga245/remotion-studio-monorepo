/* AUTO-GENERATED. Do not edit. */
import React from 'react';
import {Composition} from 'remotion';
import m0 from '../../Remotionvideo/src/manifest.ts'; // Remotionvideo-Briefing
import m1 from '../../demo-showcase/src/manifest.ts'; // DemoShowcase
import m2 from '../../hello/src/manifest.ts'; // HelloComp
import m3 from '../../playlist-mv/src/manifest.ts'; // PlaylistMV
import m4 from '../../remotion-rev/src/manifest.ts'; // RemotionRev-RemotionPages

export const AutoRoot: React.FC = () => (
  <>
  
      <Composition
        id={m0.id}
        component={m0.component}
        width={m0.config.width}
        height={m0.config.height}
        fps={m0.config.fps}
        durationInFrames={m0.config.durationInFrames}
        defaultProps={m0.defaults}
      />

      <Composition
        id={m1.id}
        component={m1.component}
        width={m1.config.width}
        height={m1.config.height}
        fps={m1.config.fps}
        durationInFrames={m1.config.durationInFrames}
        defaultProps={m1.defaults}
      />

      <Composition
        id={m2.id}
        component={m2.component}
        width={m2.config.width}
        height={m2.config.height}
        fps={m2.config.fps}
        durationInFrames={m2.config.durationInFrames}
        defaultProps={m2.defaults}
      />

      <Composition
        id={m3.id}
        component={m3.component}
        width={m3.config.width}
        height={m3.config.height}
        fps={m3.config.fps}
        durationInFrames={m3.config.durationInFrames}
        defaultProps={m3.defaults}
      />

      <Composition
        id={m4.id}
        component={m4.component}
        width={m4.config.width}
        height={m4.config.height}
        fps={m4.config.fps}
        durationInFrames={m4.config.durationInFrames}
        defaultProps={m4.defaults}
      />
  </>
);
