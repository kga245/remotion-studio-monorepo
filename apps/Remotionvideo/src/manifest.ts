import {BriefingExplainer} from './scenes/BriefingExplainer';
import {projectConfig as config} from './project.config';

const defaults = {};

export default {
  id: 'Remotionvideo-Briefing',
  tags: ['video', 'briefing'],
  component: BriefingExplainer,
  config,
  defaults,
};
