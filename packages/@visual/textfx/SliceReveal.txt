import React from "react";
import "./textfx.css";

export const KineticWords: React.FC<{words: string[]; play?: boolean; className?: string}> = ({words, play=true, className}) => (
  <div className={`demo md fx-kinetic ${className ?? ""}`} data-play={play ? "1" : undefined}>
    <div className="txt">
      {words.map((w, i) => <span key={i} className="w">{w}</span>)}
    </div>
  </div>
);
