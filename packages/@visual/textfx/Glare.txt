import React from "react";
import "./textfx.css";

export const Glare: React.FC<{text: string; play?: boolean; className?: string}> = ({text, play=true, className}) => (
  <div className={`demo lg fx-glare ${className ?? ""}`} data-play={play ? "1" : undefined}>
    <div className="noisefx" />
    <div className="txt">{text}</div>
  </div>
);
